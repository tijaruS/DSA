#include <bits/stdc++.h>

int main() {
    int n, threshold;
    std::cin >> n >> threshold;
    std::vector<int> arr(n);
    for (int i = 0; i < n; i++) {
        std::cin >> arr[i];
    }

    return 0;
}
