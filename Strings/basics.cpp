#include <bits/stdc++.h>

int main() {
    std::string s;
    std::cin >> s;
    // int i = stoi(s);
    // i += 5;
    // s = std::to_string(i);
    // s += '5';
    // std::cout << i << std::endl;
    // std::cout << s << std::endl;


    return 0;
}
